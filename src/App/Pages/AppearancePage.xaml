<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AITokenUsageWidget.App.Pages.AppearancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <ScrollViewer Padding="24,16,24,24">
        <StackPanel Spacing="12" MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="外观" Style="{StaticResource TitleTextBlockStyle}" />

            <Border Style="{StaticResource CardBorder}">
                <StackPanel Spacing="10">
                    <TextBlock Text="主题" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"
                               Text="同时作用于主界面与小组件卡片；小组件在下一次刷新（或配置变更通知）后应用。" />

                    <!-- 三段式主题选择（跟随系统 / 浅色 / 深色，FR-3） -->
                    <muxc:RadioButtons x:Name="ThemeOptions" MaxColumns="3" SelectionChanged="OnThemeChanged" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
